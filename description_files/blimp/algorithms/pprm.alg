Planner ? PPRM
Number Of PRM Vertices ? 11000
Nearest Neighbors To Consider In PRM Edge Construction ? 10
PRM Collision Check DT ? 0.05
Plaku Alpha Value ? 0.85
Plaku b Value ? 0.85
Plaku PRM State Selection Radius ? 0.1

Goal Bias ? 0.5

RRTConnect Max Extensions ? 1000